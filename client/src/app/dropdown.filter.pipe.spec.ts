import { Dropdown.FilterPipe } from './dropdown.filter.pipe';

describe('Dropdown.FilterPipe', () => {
  it('create an instance', () => {
    const pipe = new Dropdown.FilterPipe();
    expect(pipe).toBeTruthy();
  });
});
